import 'package:flutter/material.dart';

import '../constants.dart';

class Page16 extends StatelessWidget {
  static const String id = 'page_16';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: kAppBar,
    );
  }
}
